# Firestore 읽기 최적화 전체 적용 보고서

- 기준본: `user_name_korean_ime_fix_deployment_package.zip`
- 적용일: 2026-07-27
- 작업 범위: 1단계 전수 조사에서 확정한 7개 최적화 항목
- 원칙: 기능·데이터 무결성 검증에 필요한 트랜잭션 읽기는 유지하고, 화면과 무관한 상시 읽기와 전체 컬렉션 읽기를 축소

## 변경 파일

- `src/App.jsx`
- `src/admin/AdminMemberAccountsPanel.jsx`
- `firestore.indexes.json` 신규 추가

## 1. 일반 사용자의 전체 대여자 명부 구독 제거

기존에는 활성 일반회원도 `rentalBorrowers` 전체 컬렉션을 실시간 구독했습니다.

수정 후에는 다음 관리자 메뉴에서만 구독합니다.

- 부서·사용자 관리: `people`
- 회원가입 정책 관리: `signupPolicy`
- 관리자 계정 관리: `adminAccounts`

일반 사용자의 대여 신청에는 인증된 `userProfile`의 성명과 부서를 사용하므로 전체 명부를 내려받지 않습니다.

## 2. 공지사항·FAQ 화면 진입 시 조회

### 공지사항

- 사용자 홈: 상단 고정 최대 6건 + 일반 공지 최대 6건
- 사용자 공지 화면: 해당 화면에 들어갔을 때만 구독
- 관리자 공지 관리: 해당 메뉴에 들어갔을 때만 구독
- 직접 URL로 현재 페이지 밖 공지를 연 경우: 해당 문서 1건만 추가 조회

### FAQ

- 사용자 FAQ 화면에서만 카테고리·설정·게시글 구독
- 관리자 FAQ 관리 메뉴에서만 관리자용 목록 구독
- 다른 화면에서는 FAQ 컬렉션 구독을 해제하고 현재 목록 상태를 비움

## 3. 서버 제한·커서 페이지네이션 적용

다음 목록은 전체 컬렉션을 받은 뒤 `slice()`하는 방식에서 Firestore 서버 페이지 방식으로 변경했습니다.

### 관리자 대여 신청

- 현재 페이지 크기 + 다음 페이지 확인용 1건만 조회
- `startAfter()` 커서 사용
- 필터별 전체 건수는 `getCountFromServer()` 집계 사용
- 검색 시에는 최신 최대 200건만 가져온 뒤 부분 문자열 검색
- 검색어 입력은 350ms 디바운스 처리

### 관리자 회원 계정

- 페이지당 20명
- 현재 페이지 20명 + 다음 페이지 확인용 1명
- 상태 필터는 Firestore `where()` 조건으로 적용
- 전체 건수와 상태별 건수는 서버 집계 사용
- 검색 시 최신 최대 200명 범위에서 검색
- 회원 계정 관리 화면에 이전·다음 버튼 추가

### 공지사항·FAQ

- 상단 고정 게시물 최대 20건
- 일반 게시물은 설정된 페이지 크기 + 1건
- `startAfter()` 커서 사용
- 일반 게시물 전체 건수는 서버 집계 사용
- 검색은 최신 최대 200건 범위에서 수행

`offset()`은 사용하지 않았습니다. 건너뛴 문서까지 읽는 구조를 피하기 위해 문서 스냅숏 커서를 사용합니다.

## 4. 관리자 데이터 조회를 메뉴별로 분리

관리자 로그인 직후 모든 관리 컬렉션을 구독하지 않도록 변경했습니다.

- `dashboard`: 승인 대기 회원 최대 100명, 진행 중 신청 최대 100건, 상태별 집계
- `requests`: 현재 신청 페이지와 해당 신청들의 처리이력만 구독
- `memberAccounts`: 현재 회원 페이지와 그 회원들의 대여이력만 구독
- `adminAccounts`: 전체 관리자 계정은 이 메뉴에서만 구독
- `popupPosts`: 전체 팝업은 이 메뉴에서만 구독
- `noticePosts`: 공지 관리 메뉴에서만 구독
- `faqPosts`: FAQ 관리 메뉴에서만 구독
- `people`, `signupPolicy`, `adminAccounts`: 전체 대여자 명부가 실제 필요한 메뉴에서만 구독

대여 신청 처리이력은 현재 신청 페이지의 ID만 `in` 쿼리로 조회하며, 30개 단위로 분할합니다.

## 5. 중복 읽기 제거 및 무결성 읽기 구분

### 제거한 중복 읽기

- 로그인 시 이미 준비된 사용자 세션 정책을 재사용
- 세션 정책 리스너가 아직 준비되지 않은 예외 상황에서만 문서 1건 조회
- 부서·사용자 명부 저장 시 현재 관리자 화면에서 구독한 `rentalBorrowers`를 재사용
- 일반 사용자의 전체 `rentalBorrowers` 구독 제거

### 의도적으로 유지한 읽기

다음은 단순 표시용 읽기가 아니라 동시성 또는 색인 무결성 검증이므로 유지했습니다.

- 신청·승인·반납 트랜잭션의 최신 신청·자산·설정·제한 문서 읽기
- 부서·사용자 명부 저장 및 전체 명부 감사 시 회원·identity claim·복구 키 전체 검증
- 자산 카테고리 변경 시 자산 및 활성 예약 연계 검증
- 관리자 계정 프로필 저장 시 관리자 ID 중복 검증

### 연체 반납 판정 보완

관리자 신청 목록이 현재 페이지만 보유하게 되면서, 메모리 배열만으로 같은 사용자의 다른 연체 건을 판단하면 누락될 수 있습니다.

반납 처리 시에만 다음 조건으로 최대 2건을 제한 조회합니다.

- 동일 사용자 UID
- 대여 중 상태
- 반납 처리일보다 반납예정일이 이전
- 현재 반납 건 제외

평상시 화면 조회에는 이 추가 읽기가 발생하지 않습니다.

## 6. 리스너 의존성과 검색 재조회 안정화

- 리스너 의존성에서 관리자 계정 객체 전체 대신 UID·문서 ID·인증 boolean 등 안정적인 값 사용
- 객체 참조 변경으로 전체 컬렉션이 재구독되는 가능성 축소
- 검색 입력에 350ms 디바운스 적용
- 페이지를 이동할 때 전체 건수 집계를 반복하지 않고, 필터·검색 모드가 바뀔 때만 다시 집계
- 화면에서 나갔다가 다시 진입하면 커서를 초기화해 오래된 페이지 커서를 재사용하지 않음

## 7. 자산·예약 현황을 필요한 화면에서만 구독

### 사용자 화면

- 홈
- 대여 신청

### 관리자 화면

- 대시보드
- 대여 자산 관리
- 대여 신청 관리

그 외 로그인·회원가입·마이페이지·게시판·설정 화면에서는 `rentalAssets`와 `rentalAvailability` 리스너를 유지하지 않습니다.

## 추가 안전 보완

- FAQ 카테고리 삭제 전 현재 페이지 배열이 아니라 서버 집계로 해당 카테고리 사용 건수를 확인
- 현재 페이지 밖 공지 직접 접근 시 목록 전체를 읽지 않고 해당 문서 1건 조회
- 가입 제한 정책 해제 후 자동 복원은 `profileRequired + directoryMismatch` 대상만 조건 조회
- 회원 명부 저장·감사처럼 전체 일관성이 필요한 관리자 작업은 명시적으로 전체 조회
- 일반 목록의 부분 데이터와 전역 무결성 검증용 전체 데이터를 구분

## Firestore 복합 인덱스

신규 `firestore.indexes.json`에는 다음 쿼리를 위한 14개 복합 인덱스를 정의했습니다.

- 신청 상태 + 생성일 오름차순/내림차순
- 신청 상태 + 반납예정일
- 신청 상태 + 시작일
- 오늘 반납·오늘 시작 조건 + 생성일 내림차순
- 사용자 UID + 상태 + 반납예정일
- 사용자 요청 검토 상태 + 요청일
- 회원 상태 + 생성일 오름차순/내림차순
- 공지 고정 여부 + 생성일
- FAQ 카테고리·고정 여부·생성일 조합

프런트엔드 배포 전에 기존 Firebase 프로젝트 설정에서 이 인덱스 파일을 배포해야 합니다.

```bash
firebase deploy --only firestore:indexes
```

프로젝트의 `firebase.json`에 Firestore 인덱스 경로가 없다면 기존 설정을 유지하면서 다음 항목을 병합해야 합니다.

```json
{
  "firestore": {
    "rules": "rules/firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
```

기존 `firebase.json` 전체를 위 내용으로 덮어쓰면 Hosting 등 다른 설정이 사라질 수 있으므로 병합해야 합니다.

## 운영상 제한 및 의도된 절충

- 부분 문자열 검색은 Firestore가 직접 지원하지 않으므로 최신 최대 200건 범위에서 로컬 검색합니다.
- 관리자 대시보드의 상세 목록은 승인 대기 회원 최대 100명, 진행 중 신청 최대 100건입니다. 상태별 숫자는 서버 집계를 사용합니다.
- 고정 공지·FAQ는 각각 최대 20건입니다.
- 관리자 회원 페이지의 대여 이력은 현재 표시된 회원과 이전 계정 UID에 한해 조회합니다.
- 페이지형 목록에서 전체 데이터에 대한 고아 예약 진단은 정확성을 보장할 수 없어 일반 화면 계산에서 제외했습니다. 전체 무결성 검사는 별도의 명시적 관리자 점검 작업으로 수행해야 합니다.
- `createdAt`이 없는 오래된 문서는 `orderBy('createdAt')` 쿼리 결과에서 제외될 수 있습니다. 운영 데이터에 누락된 `createdAt`이 있는지 배포 전에 확인하는 것이 안전합니다.

## 예상 효과

정확한 절감량은 실제 문서 수, 사용자 접속 화면, 실시간 변경 횟수에 따라 달라집니다. 구조적으로는 다음 읽기가 제거되거나 제한됩니다.

- 로그인·게시판·마이페이지에서 자산과 예약 전체 읽기
- 일반 사용자의 대여자 전체 명부 읽기
- 관리자 로그인 직후 회원·신청·이력 전체 읽기
- 공지·FAQ 전체 읽기
- 관리자 목록의 전체 컬렉션 읽기 후 브라우저 페이지 분할
- 동일 설정 문서의 로그인 단계 중복 읽기
- 페이지 이동마다 반복되는 전체 건수 집계

## 배포 순서

1. 현재 운영 프로젝트 백업
2. `firestore.indexes.json`을 프로젝트 루트에 배치
3. 기존 `firebase.json`에 인덱스 경로 병합
4. `firebase deploy --only firestore:indexes`
5. Firebase Console에서 인덱스 생성 완료 확인
6. 변경된 `src` 파일 배포
7. 사용자 홈·대여 신청·공지·FAQ 점검
8. 관리자 대시보드·신청·회원·공지·FAQ·팝업 메뉴 점검
9. Firestore 사용량 대시보드에서 일별 읽기 추이 확인
