# 관리자 대시보드 요약 문서 최적화 보고서

## 기준본

- `rental-system-latest-baseline.zip`
- 사용자가 수정한 페이지 텍스트와 전체 프로젝트 구조를 그대로 기준으로 사용함

## 작업 목적

관리자 대시보드 진입 시 여러 컬렉션을 동시에 실시간 구독하던 구조를 `laptopRentalDashboard/main` 단일 요약 문서 중심으로 전환한다.

## 수정 파일

1. `src/App.jsx`
2. `src/admin/AdminDashboardPanel.jsx`
3. `src/firebase.js`
4. `rules/firestore.rules`

## 주요 변경

### 1. 단일 요약 문서 참조 추가

- 경로: `laptopRentalDashboard/main`
- Firebase 참조: `DASHBOARD_SUMMARY_DOC_REF`

### 2. 대시보드 전용 실시간 컬렉션 구독 제거

관리자 대시보드에서 다음 구독을 제거했다.

- `rentalAssets` 전체 구독
- `rentalAvailability` 전체 구독
- 진행 중 `rentalRequests` 최대 100건 실시간 구독
- 승인 대기 `userAccounts` 최대 100건 실시간 구독
- 대시보드 진입 시 별도 상태별 집계 반복 실행

자산 관리, 신청 관리, 회원 관리 화면의 기존 조회와 서버 페이지네이션은 변경하지 않았다.

### 3. 요약 생성 방식

요약이 없거나 오래됐을 때 관리자 클라이언트가 다음 자료를 일회성으로 읽고 요약 문서를 생성한다.

- 진행 신청 상세: 최대 100건
- 승인 대기 회원 미리보기: 최대 12건
- 공개 자산 카탈로그: 1문서
- 신청 및 회원 상태별 `count()` 집계

요약에 저장되는 항목:

- 진행 신청 미리보기
- 승인 대기 회원 미리보기
- 신청 탭별 전체 건수
- 연체, 오늘 반납, 오늘 시작, 사용자 요청 건수
- 전체/대여 가능/예약/대여/대여 불가 자산 지표
- 데이터 이상 건수
- 생성 시각, 기준일, 생성 관리자 UID

### 4. 갱신 정책

- 요약 문서가 없으면 즉시 생성
- 요약 기준일이 오늘이 아니면 즉시 갱신
- 요약 생성 후 2분이 지나면 대시보드 세션에서 1회 자동 갱신
- 신청 관리, 회원 관리, 자산 관리, 카테고리, 데이터 관리 화면에서 대시보드로 돌아오면 강제 갱신
- 헤더의 수동 갱신 버튼으로 즉시 갱신 가능
- 다른 관리자가 요약 문서를 갱신하면 단일 문서 리스너로 즉시 반영

### 5. 공개 자산 카탈로그 복구

`publicCatalog/main`이 없거나 형식이 잘못된 경우 대시보드 요약 생성 과정에서 `rentalAssets`를 한 번 읽고 공개 카탈로그도 함께 복구한다.

### 6. 보안 규칙

`laptopRentalDashboard/main`은 다음 정책을 적용한다.

- 읽기: 관리자만 허용
- 생성·수정: 관리자만 허용
- `updatedByUid`는 로그인한 관리자 UID와 일치해야 함
- 진행 신청 배열 최대 100건
- 승인 대기 회원 배열 최대 12건
- 필수 자료형 검증
- 삭제 금지

## 인덱스

새 복합 인덱스는 추가하지 않았다. 기존 `firestore.indexes.json`에 필요한 인덱스가 이미 존재한다.

## 읽기 구조 비교

### 수정 전

대시보드 진입 시 다음 자료가 각각 초기 읽기와 실시간 변경 읽기를 발생시킬 수 있었다.

- 자산 전체
- 예약 현황 전체
- 진행 신청 최대 100건
- 승인 대기 회원 최대 100건
- 상태별 집계

### 수정 후

요약이 최신이면:

- `laptopRentalDashboard/main` 1문서 읽기

요약 갱신 시:

- 진행 신청 최대 100건
- 승인 대기 회원 최대 12건
- 공개 자산 카탈로그 1건
- 10개 집계 쿼리
- 공개 카탈로그가 없을 때만 자산 원본 일회성 조회

## 제한 사항

- Cloud Functions를 사용하지 않는 Spark 요금제 구조이므로 요약 문서는 서버 트리거가 아니라 관리자 클라이언트가 갱신한다.
- 상태별 전체 숫자는 집계 쿼리 결과다.
- 진행 신청이 100건을 초과하면 상세 목록과 일부 고유 사용자·자산 보조 지표는 최근 100건 기준이며 UI에 경고를 표시한다.
- 관리자가 대시보드를 장시간 열어 둔 경우 최초 자동 갱신 이후에는 헤더의 수동 갱신 버튼으로 최신화할 수 있다.
