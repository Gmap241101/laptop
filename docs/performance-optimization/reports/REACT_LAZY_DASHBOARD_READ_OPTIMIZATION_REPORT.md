# 관리자 React.lazy 및 대시보드 읽기 정책 수정 보고서

## 기준본

- `rental-system-dashboard-summary-optimization-deployment-package.zip`
- 기존 사용자 페이지 문구와 프로젝트 구조 유지

## 1. 대시보드 요약 갱신 읽기 비용 확인

기존 구현은 관리자 대시보드가 열린 동안 2분마다 `refreshDashboardSummary()`를 실행했다.

1회 갱신 시 실행되는 읽기 요청:

- 진행 신청 미리보기 쿼리: 최대 100문서
- 승인 대기 회원 미리보기 쿼리: 최대 12문서
- 공개 자산 카탈로그 단일 문서: 1문서
- `getCountFromServer()` 집계 쿼리: 10개
- 공개 카탈로그가 없거나 비정상일 때만 `rentalAssets` 전체 일회성 조회

집계 쿼리는 일치하는 색인 항목 최대 1,000개당 읽기 1회이며, 결과가 없어도 쿼리별 최소 읽기 1회가 적용된다. 따라서 공개 카탈로그가 정상인 일반 상황에서 1회 갱신의 기본 읽기는 대략 다음과 같다.

`진행 신청 반환 문서 수 + 승인 대기 회원 반환 문서 수 + 1 + 각 집계 쿼리 색인 읽기 합계`

- 반환 문서가 모두 0이고 집계별 색인 항목이 1,000개 이하인 최소 사례: 약 13회
- 진행 신청 100건, 승인 대기 회원 12건, 집계별 색인 항목이 1,000개 이하인 사례: 약 123회
- Firestore Rules가 관리자 문서를 `get()` 또는 `exists()`로 확인하면 요청별 종속 문서 읽기가 추가될 수 있음

2분 간격으로 8시간 대시보드를 열어두면 240회 갱신된다.

- 최소 사례: 약 3,120회
- 미리보기 상한 사례: 약 29,520회
- 위 수치에는 보안 규칙 종속 읽기와 카탈로그 복구 읽기가 포함되지 않음

따라서 기존 2분 자동 갱신은 읽기 절감 목적에 부합하지 않는 것으로 확인됐다.

## 2. 대시보드 갱신 정책 수정

### 수정 전

- 대시보드 요약 유효시간: 2분
- 대시보드가 열린 동안 2분마다 자동 원본 집계

### 수정 후

- 대시보드 진입 시 요약이 15분 이상 오래됐을 때 1회 갱신
- 요약 문서가 없거나 스키마·기준일이 맞지 않을 때 1회 갱신
- 신청·회원·자산·카테고리·데이터 관리 화면에서 대시보드로 돌아올 때 1회 갱신
- 관리자가 `요약 갱신` 버튼을 누를 때 갱신
- 대시보드를 계속 열어둔 상태에서는 주기적 자동 갱신 없음
- 요약 문서 실시간 구독 자체는 유지하므로 다른 관리자가 요약을 갱신하면 문서 1건 변경을 수신

### 정확성·비용 절충

주기적 폴링을 제거했으므로 대시보드를 계속 열어둔 동안 일반 사용자가 새 신청을 제출해도 요약 숫자는 즉시 자동 재집계되지 않는다. 필요한 경우 관리자가 수동 갱신 버튼을 사용한다. 이는 Cloud Functions 없이 Spark 요금제에서 반복 읽기를 줄이기 위한 의도된 정책이다.

## 3. 관리자 화면 코드 분할

### 최상위 관리자 작업공간

`src/App.jsx`의 정적 import를 제거하고 다음 동적 import로 교체했다.

```jsx
const AdminWorkspace = React.lazy(() => import('./admin/AdminWorkspace.jsx'));
```

사용자 화면에서는 `AdminWorkspace` 모듈을 요청하지 않는다. 관리자 화면으로 전환할 때 처음 로드한다.

### 관리자 세부 패널

`src/admin/AdminWorkspace.jsx`의 관리자 패널 정적 import 17개를 모두 `lazy(() => import(...))`로 교체했다.

- 대시보드
- 신청 관리
- 자산 관리
- 휴일 관리
- 카테고리 관리
- 부서·사용자 관리
- 회원가입 정책
- 공지사항
- 사이트 설정
- 홈 화면 관리
- 팝업
- FAQ
- 푸터
- 회원 계정
- 관리자 계정
- 계정 보안
- 대여 정책

`AdminSettingsPanel`은 서비스 운영·사이트 설정·데이터 관리·시스템 정보에서 동일 동적 모듈을 재사용한다.

### Suspense 처리

- 관리자 작업공간 로딩 fallback 추가
- 관리자 세부 패널 로딩 fallback 추가
- `lazy()` 선언은 컴포넌트 외부 모듈 최상위에 배치하여 재렌더링 시 컴포넌트 상태가 초기화되지 않도록 처리

## 4. 기대 효과와 한계

### 기대 효과

- 사용자 화면 초기 모듈 그래프에서 관리자 작업공간 제외
- 관리자 모드에서도 선택하지 않은 세부 패널 코드 로딩 지연
- 큰 관리자 패널을 필요한 시점에만 다운로드·파싱·실행
- 관리자 패널 정적 import 17개에서 0개로 감소
- 동적 분기점 18개 생성

### 한계

`App.jsx`가 약 659KB이고 관리자 상태·Firestore 처리 함수 상당수가 여전히 이 파일 안에 있다. 따라서 이번 단계에서는 관리자 패널 UI 파일 약 538KB를 지연 로딩하지만, 관리자 업무 로직 전체가 초기 번들에서 빠지는 것은 아니다. 다음 우선순위 8의 `App.jsx` 구조 분리에서 서비스·상태·관리자 액션을 별도 모듈과 훅으로 이동해야 추가적인 번들 감소와 재렌더링 축소가 가능하다.

## 5. 변경 파일

- `src/App.jsx`
- `src/admin/AdminWorkspace.jsx`

Firebase Rules와 인덱스는 변경하지 않았다.

## 6. 배포

이번 변경은 프런트엔드 코드만 수정하므로 Firebase Rules 재배포는 필요 없다.

```powershell
Set-Location "E:\project\rental-system\test_new"
.\deploy.ps1
```
