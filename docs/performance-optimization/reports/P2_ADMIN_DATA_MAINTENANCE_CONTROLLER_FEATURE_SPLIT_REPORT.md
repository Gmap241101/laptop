# P2 관리자 데이터 유지보수 컨트롤러 분리 보고서

## 1. 기준본과 작업 범위

- 입력 기준본: `rental-system-admin-split-storage-controller-holiday-save-fix-20260730_1728_deployment_package.zip`
- 작업 대상: `src/admin/AdminSettingsPanel.jsx`의 데이터 점검·백업·복원·초기화 상태와 실행 로직
- 신규 모듈: `src/features/settings/useAdminDataMaintenanceController.js`
- UI, Firestore 문서 구조, Rules, 인덱스, 사용자 문구 변경: 없음
- `src/App.jsx`: 이번 단계에서 변경 없음

직전 단계에서 분리 저장소 최종 전환 로직은 이미 `useAdminSplitStorageMigrationController.js`로 이동했습니다. 이번 단계는 같은 순차 계획에서 남아 있던 JSON 백업·복원, 데이터 건전성 점검과 위험 초기화 기능을 관리자 설정 패널에서 분리한 작업입니다.

## 2. 분리된 상태

다음 31개의 React 상태를 신규 컨트롤러로 이동했습니다.

- 시스템 데이터 점검 로딩·결과
- 백업 로딩 및 운영·회원·개인정보 포함 옵션
- 초기화 범위, 대상 수, 스캔·실행·진행 상태, 관리자 비밀번호, 확인 문구, 사전 백업 완료 여부
- 최근 초기화 작업
- 복원 파일명·해시·payload·검증·충돌 분석
- 복원 분석·실행·진행 상태와 복원 방식·범위
- 프로젝트 불일치 강제 복원 상태
- 복원 관리자 비밀번호·확인 문구
- 최근 복원 작업과 복원 결과

`AdminSettingsPanel.jsx`에는 화면 렌더링에 필요한 반환값과 이벤트 함수 연결만 남겼습니다.

## 3. 분리된 실행 기능

### 시스템 데이터 점검

- 자산, 자산관리번호 레지스트리, 예약 잠금, 대여신청, 회원, 식별 claim, 계정 복구키, 대여 제한 전체 점검
- 공개 자산 카탈로그 fingerprint·건수 비교
- 존재하지 않는 자산·회원 참조 검출
- 진행 중 신청 날짜 누락과 기간 역전 검출
- 고아 예약 잠금, 탈퇴회원 식별키, 누락 claim·복구키·이메일 검증값, 만료 제재 검출
- 점검 결과 및 감사 로그 저장

### JSON 백업

- 설정·운영·회원 컬렉션 선택 백업
- 개인정보 포함 여부에 따른 문서 마스킹
- 고정 설정 문서 백업
- 백업 metadata와 컬렉션별 건수 생성
- JSON 다운로드 및 최근 백업 정보·감사 로그 저장

### JSON 복원

- JSON 파일 형식·크기·프로젝트·스키마 검증
- 복원 범위와 방식 선택
- 문서 충돌·누락·추가 문서 분석
- 자산·회원 참조와 자산관리번호 중복 검증
- 타 프로젝트 강제 복원 확인
- 관리자 재인증
- 복원 직전 개인정보 포함 전체 백업
- 중단 작업 이어서 실행
- 기존 문서 삭제, 문서·컬렉션 복원, 공개 자산 카탈로그 재생성
- 복원 중·완료 후 점검 모드 유지
- 데이터 건전성 점검 후 관리자·사용자 보안 정책을 최종 원자 배치로 적용
- 복원 작업·감사 로그·완료 요약 저장

### 데이터 초기화

- 최고 관리자 권한과 현재 비밀번호 재확인
- 초기화 전 개인정보 포함 전체 백업 강제
- 선택 범위별 문서 수 스캔
- 자산·회원 단독 초기화 시 잔존 대여내역 검사
- 일반회원 Auth UID 및 로컬 Admin SDK 정리 스크립트 다운로드
- 컬렉션별 배치 삭제와 중단 작업 재개
- 콘텐츠 설정 문서 제거
- 명부·회원가입 정책·자산 카테고리 관련 공개 설정 정리
- 공개 자산 카탈로그 재생성
- 공장 초기화 시 사이트 설정 기본값 복원과 점검 모드 유지
- 작업 기록·감사 로그·완료 안내 저장

## 4. 관리자 설정 패널 변화

| 항목 | 수정 전 | 수정 후 | 변화 |
|---|---:|---:|---:|
| 줄 수 | 2,534 | 1,098 | -1,436 |
| 크기 | 121,164 bytes | 63,945 bytes | -57,219 bytes |
| `useState()` | 36 | 5 | -31 |
| `getDocs()` | 17 | 0 | -17 |
| `getDoc()` | 6 | 0 | -6 |
| `onSnapshot()` | 3 | 1 | -2 |

신규 컨트롤러는 1,606줄, 61,030 bytes입니다. 위험 데이터 유지보수 로직을 화면 컴포넌트와 분리해 후속 서비스 세분화와 독립 검증이 가능하도록 했습니다.

## 5. App.jsx 및 초기 소스 그래프

`App.jsx`는 직전 기준본과 동일합니다.

| 항목 | 수정 전 | 수정 후 | 변화 |
|---|---:|---:|---:|
| `App.jsx` 줄 수 | 10,176 | 10,176 | 0 |
| `App.jsx` 크기 | 296,485 bytes | 296,485 bytes | 0 |

`AdminSettingsPanel`은 기존부터 관리자 설정 화면의 지연 로딩 경로에 포함되어 있어 초기 정적 소스 그래프에는 변화가 없습니다.

| 항목 | 수정 전 | 수정 후 | 변화 |
|---|---:|---:|---:|
| 초기 정적 모듈 | 55 | 55 | 0 |
| 초기 정적 소스 | 810,442 bytes | 810,442 bytes | 0 |

## 6. Firestore 영향

- 전체 감사 대상 호출: 129 → 129
- `onSnapshot`: 35 → 35
- `getDocs`: 48 → 48
- `getDoc`: 28 → 28
- `getCountFromServer`: 18 → 18
- 데이터 유지보수용 전체 조회 17개의 감사 ID만 신규 소스 위치에 맞게 갱신
- 기존 승인 사유와 재검토 조건 변경 없음
- Firestore Rules·인덱스·Firebase 설정 변경 없음

## 7. 최근 작업 구독 분리

기존 `AdminSettingsPanel`의 통합 effect에서 초기화·복원 작업 구독을 제거하고 신규 컨트롤러 effect로 이동했습니다.

- 시스템 정보 모드의 감사 로그 구독은 패널에 유지
- 데이터 관리 모드의 최근 초기화 작업 구독은 컨트롤러로 이동
- 데이터 관리 모드의 최근 복원 작업 구독은 컨트롤러로 이동
- 최고 관리자가 아닌 경우 초기화 작업 구독을 생성하지 않는 기존 조건 유지

## 8. 삭제 파일

- 이번 작업 신규 삭제 경로: 0개
- 기존 `package-meta/REMOVED_FILES.txt` 누적 목록 그대로 승계
