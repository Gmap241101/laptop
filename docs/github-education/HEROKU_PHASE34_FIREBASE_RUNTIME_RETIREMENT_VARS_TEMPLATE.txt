# Phase 34 Firebase-free runtime
# No Firebase runtime Config Vars are required.
# After deployment validation, remove obsolete FIREBASE_* and FIRESTORE_* compatibility variables from Staging.
# Keep DATABASE_URL, DATABASE_SSL_MODE, CLERK_SECRET_KEY, CLERK_JWT_KEY, CLERK_AUTHORIZED_PARTIES, APP_ENV and SERVICE_* variables.
