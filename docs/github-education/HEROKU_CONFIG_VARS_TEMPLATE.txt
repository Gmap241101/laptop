# Phase 7 configuration reference only.
# Do NOT rename this file to .env* inside a deployment package.
# Heroku values belong in Heroku Dashboard/CLI Config Vars.
# Vercel frontend values belong in Vercel Project Environment Variables.

# Frontend (configure in Vercel, not Heroku)
VITE_CLERK_STAGING_ENABLED=true
VITE_API_URL=https://YOUR-HEROKU-STAGING-APP.herokuapp.com
VITE_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# Backend runtime (Heroku Config Vars)
APP_ENV=staging
SERVICE_NAME=rental-api
SERVICE_VERSION=phase31
# PORT is local-only. Do not set PORT manually in Heroku.

# PostgreSQL connection. Heroku Postgres manages DATABASE_URL automatically.
DATABASE_URL=HEROKU_MANAGED_DO_NOT_COPY
DATABASE_SSL_MODE=require
DB_POOL_MAX=5
DB_CONNECTION_TIMEOUT_MS=5000
DB_IDLE_TIMEOUT_MS=10000

# Exact comma-separated frontend origins.
CORS_ALLOWED_ORIGINS=https://YOUR-STAGING-PROJECT.vercel.app

# Clerk Development instance JWT public key (PEM), stored only as a Heroku Config Var.
CLERK_JWT_KEY="-----BEGIN PUBLIC KEY-----\nREPLACE_WITH_CLERK_JWT_PUBLIC_KEY\n-----END PUBLIC KEY-----"
CLERK_AUTHORIZED_PARTIES=https://YOUR-STAGING-PROJECT.vercel.app
CLERK_CLOCK_SKEW_SECONDS=5
CLERK_REJECT_PENDING_SESSION=true

# Phase 5: Clerk Backend API access. Backend-only secret; never prefix with VITE_.
CLERK_SECRET_KEY=sk_test_REPLACE_ME
# Optional. Defaults shown below; normally leave CLERK_API_URL unset in Heroku.
CLERK_API_URL=https://api.clerk.com/v1
CLERK_API_TIMEOUT_MS=8000


# Phase 6: verify the existing Firebase Authentication ID token on Heroku.
# This is a public project identifier, not a secret.
FIREBASE_PROJECT_ID=laptop-system-mk
# Optional; defaults to 8000 ms. Controls Google signing-certificate fetch timeout.
FIREBASE_CERT_TIMEOUT_MS=8000


# Phase 7: Firestore userAccounts read-only shadow sync via REST using the current user's Firebase ID token.
# No service-account key is required; Firestore Security Rules remain authoritative for this read.
# Optional; defaults to 8000 ms.
FIRESTORE_REST_TIMEOUT_MS=8000


# Phase 14: rentalRequests shadow sync/read uses the existing FIREBASE_PROJECT_ID and
# FIRESTORE_REST_TIMEOUT_MS. The caller's verified Firebase ID token is forwarded to
# Firestore REST; do not add a Firebase Admin/service-account private key for this phase.
# No new backend secret or config variable is required by Phase 14 or Phase 15.

# Phase 17: no new backend secret is required.
# After deploying the Phase 17 server source, set the diagnostic version marker to:
# SERVICE_VERSION=phase25
# Migration 009 is applied by the existing Procfile release phase.
# Admin rental-request bootstrap/status mirror continues to use the verified caller Firebase ID token through Security Rules.


# Phase 20: asset-domain PostgreSQL read/write cutover.
# No new backend secret is required. Migration 012 creates the normalized asset/category catalog.
# Existing FIREBASE_PROJECT_ID/FIRESTORE_REST_TIMEOUT_MS are reused for administrator bootstrap
# and Firestore compatibility mirrors under the current Firebase administrator token.
# Recommended diagnostic version marker: SERVICE_VERSION=phase25
# Phase 21: member/restriction authoritative write + administrator identity registry preparation.
# No new backend secret is required. Migration 013 is executed by the existing Procfile release phase.
# Existing Firebase ID tokens remain the compatibility-mirror/admin authorization credential for this phase.
# Recommended diagnostic version marker: SERVICE_VERSION=phase25


# Phase 22: PostgreSQL-preferred account recovery + Clerk-authoritative administrator authentication.
# No new backend secret is required. Migration 014 is executed by the existing Procfile release phase.
# Existing CLERK_SECRET_KEY drives administrator Clerk create/update/link operations.
# Existing Firebase ID tokens remain a compatibility credential for Firestore administrator proof during staged cutover.
# Recommended diagnostic version marker: SERVICE_VERSION=phase25


# Phase 23: user Clerk authentication + account lifecycle authority.
# No new backend secret is required. Migration 015 is executed by the existing Procfile release phase.
# Existing CLERK_SECRET_KEY is reused for user create/verify/update/delete operations.
# Existing Firebase ID tokens remain compatibility credentials while Firestore Rules, reset delivery,
# and legacy lifecycle documents are still present during the staged cutover.
# Recommended diagnostic version marker: SERVICE_VERSION=phase25
\n\n# Phase 24: site shell content PostgreSQL preferred read + Firestore admin write-through.\n# No new backend secret is required. Migration 016 is executed by the existing Procfile release phase.\n# Existing Clerk/Firebase administrator credentials authorize the write-through sync endpoint.\n# Recommended diagnostic version marker: SERVICE_VERSION=phase25\n
# Phase 25: rental public config + signup terms PostgreSQL preferred reads with Firestore transaction authority preserved.
# No new backend secret is required. Migration 017 records the runtime contract.
# Recommended diagnostic version marker: SERVICE_VERSION=phase25

# Phase 26: notice/FAQ PostgreSQL read + PostgreSQL-authoritative admin CRUD with Firestore compatibility mirror.
# No new backend secret is required. Migration 018 runs in the existing Procfile release phase.
# Recommended diagnostic version marker: SERVICE_VERSION=phase26

# Phase 27: frontend-only legacy Firestore read fallback retirement.
# No Heroku source/config change; keep SERVICE_VERSION=phase26.

# Phase 28: asset + notice/FAQ PostgreSQL-authoritative write mirror retirement.
# Backend authority flag. Keep Firebase Auth/admin identity verification enabled.
FIRESTORE_ASSET_BOARD_WRITE_MIRROR_DISABLED=true
# Recommended diagnostic version marker: SERVICE_VERSION=phase28


# Phase 29: rental transaction PostgreSQL authority + rental-request Firestore write mirror retirement.
# Keep Firebase administrator compatibility identity and legacy bootstrap/sync available for rollback.
FIRESTORE_RENTAL_REQUEST_WRITE_MIRROR_DISABLED=true
# Recommended diagnostic version marker: SERVICE_VERSION=phase29

# Phase 30: administrator member-status + status-related rental-restriction PostgreSQL authority.
# Retires Firestore write mirrors only for administrator member status changes and the inherited
# restriction written by that status transaction. Member profile edit mirrors remain enabled.
FIRESTORE_MEMBER_STATUS_RESTRICTION_WRITE_MIRROR_DISABLED=true
# Recommended diagnostic version marker: SERVICE_VERSION=phase30


# Phase 31: member profile identity/recovery PostgreSQL authority.
# Normal user/admin profile edits no longer use Firestore userAccounts/identity claims/recovery keys as runtime source/mirror.
FIRESTORE_MEMBER_PROFILE_WRITE_MIRROR_DISABLED=true
# Recommended diagnostic version marker: SERVICE_VERSION=phase31


# Phase 32: signup profile/bootstrap + terms consent PostgreSQL account lifecycle authority.
# Firebase Auth account creation/session and password-reset delivery remain as compatibility until final Firebase Auth retirement.
FIRESTORE_ACCOUNT_LIFECYCLE_COMPATIBILITY_DISABLED=true
# Recommended diagnostic version marker: SERVICE_VERSION=phase32
