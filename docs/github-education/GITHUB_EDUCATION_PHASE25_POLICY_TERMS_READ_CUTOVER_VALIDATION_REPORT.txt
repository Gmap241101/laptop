Phase 25 validation contract

Expected automated checks:
- Migration 017 static markers
- rental-config and terms backend content-domain smoke
- publicConfig PostgreSQL preferred read + Firestore one-time fallback
- signup terms PostgreSQL preferred read + Firestore fallback
- administrator terms Firestore write + PostgreSQL write-through
- administrator post-login /admin route stabilization
- Phase 1-24 regression preservation
- React hook audit
- Application flow audit
- Firestore strict audit
- Package guard + ZIP SHA verification

Actual staging validation remains required before Phase 25 can become PASS baseline.

Phase 25 visual content synchronization hotfix candidate
- Staging issue observed: user popup/main visual/promotion/quick-link content missing while PostgreSQL site-content read was active.
- Observed user home PostgreSQL document count: 2.
- Administrator site/policy writes and `/admin` routing: actual staging PASS before hotfix.
- Hotfix: administrator site-content bootstrap/write-through now forces Firestore server snapshots (`getDocFromServer` / `getDocsFromServer`).
- Hotfix: PostgreSQL sync response document count must equal the Firestore server snapshot count.
- Hotfix: user home/popup staging reads compare enabled ID + updatedAt signatures and use one-time Firestore server fallback on mismatch.
- Diagnostics: PostgreSQL and Firestore server document counts are shown separately.
- Firestore audit: server-only get functions are now counted in the existing getDoc/getDocs totals.
- Actual browser revalidation is required before Phase 25 can become PASS baseline.
