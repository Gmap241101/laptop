# Production API custom domain contract
# Frontend origin (GitHub Pages custom domain)
PRODUCTION_FRONTEND_ORIGIN=https://notebook.recruit.kro.kr

# Frontend build-time API origin
VITE_API_URL=https://api.notebook.recruit.kro.kr

# Production Heroku backend config vars
APP_ENV=production
CORS_ALLOWED_ORIGINS=https://notebook.recruit.kro.kr
CLERK_AUTHORIZED_PARTIES=https://notebook.recruit.kro.kr

# DNS contract
# api.notebook.recruit.kro.kr CNAME -> <Heroku-assigned-xxxx.herokudns.com>
# Never point the custom domain directly at *.herokuapp.com when using Heroku ACM.

# Production publish guard (set only after explicit production approval)
CONFIRM_PRODUCTION_DEPLOY=notebook.recruit.kro.kr

# IMPORTANT
# - Keep public/CNAME as notebook.recruit.kro.kr. It is the frontend GitHub Pages domain.
# - Do not commit .env/.env.production files. This project excludes them from deployment packages.
# - Production Clerk key/cutover is a separate required production step. The current Phase 34
#   frontend still contains legacy VITE_CLERK_STAGING_ENABLED / pk_test_ assumptions and must
#   not be treated as Production Clerk-ready solely because the API custom domain is ready.
