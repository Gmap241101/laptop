# Phase 34 Staging frontend — Firebase normal-runtime retirement
VITE_CLERK_STAGING_ENABLED=true
VITE_FIREBASE_RUNTIME_DISABLED=true
VITE_ADMIN_CONTENT_POSTGRES_AUTHORITY_ENABLED=true

# Preserve the Phase 32/33 PASS variables. The global flag also forces these
# authorities at runtime, but keeping the explicit values makes rollback clear.
VITE_USER_FIREBASE_AUTH_COMPATIBILITY_DISABLED=true
VITE_SITE_CONTENT_POSTGRES_AUTHORITY_ENABLED=true
VITE_POLICY_CONTENT_POSTGRES_AUTHORITY_ENABLED=true
VITE_ACCOUNT_LIFECYCLE_POSTGRES_AUTHORITY_ENABLED=true

# Deliberate browser-session diagnostic rollback only:
# ?firebaseRuntime=compatibility
