# Phase 34 Firebase-free runtime
# No VITE_FIREBASE_* / VITE_FIRESTORE_* runtime variable is required by the hard-retired source.
# Keep VITE_API_URL, VITE_CLERK_PUBLISHABLE_KEY and VITE_CLERK_STAGING_ENABLED for Staging.
# Legacy Phase cutover variables may be removed after Staging validation because Phase 34 hard-wires PostgreSQL/Clerk authority.
