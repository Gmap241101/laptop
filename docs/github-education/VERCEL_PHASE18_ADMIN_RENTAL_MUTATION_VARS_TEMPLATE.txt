Phase 18 Vercel Production Environment variables for the dedicated mkrental Staging project

No new Phase 18 key is required.
Reuse:
VITE_ADMIN_RENTAL_REQUEST_POSTGRES_READ_ENABLED=true
VITE_ADMIN_RENTAL_REQUEST_POSTGRES_WRITE_ENABLED=true

Keep the previously validated Phase 14-16 variables enabled.
Do not add DATABASE_URL, CLERK_SECRET_KEY, CLERK_JWT_KEY, or Firebase service-account private keys to VITE_* variables.

Deploy/redeploy the Phase 18 source after applying the package.

Explicit browser opt-in:
?clerkTest=1&memberRead=postgres&memberWatcher=off&memberWriteThrough=on&restrictionRead=postgres&restrictionWatcher=off&rentalRequestRead=postgres&rentalRequestWatcher=off&rentalRequestWrite=postgres&adminRequestRead=postgres&adminRequestWrite=postgres
