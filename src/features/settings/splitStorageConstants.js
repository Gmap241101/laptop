// Historical schema marker retained because readiness selectors still compare against it.
// Phase 34 uses PostgreSQL as the only active runtime storage authority.
export const SPLIT_STORAGE_VERSION = 2;
