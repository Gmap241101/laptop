사용자 홈 대여 현황 전용 레이아웃 복원 추가 배포 안내

기준 패키지
- rental_policy_calendar_days_status_ui_fix_deployment_package.zip

이번 추가 변경 파일
- src/user/UserHomePanel.jsx

수정 내용
- 사용자 홈의 오늘의 대여 현황은 관리자 상단과 같은 독립 카드 6개 구조를 사용하지 않습니다.
- 이전 사용자 홈 전용 구조로 복원했습니다.
  · 왼쪽 남색 제목 영역
  · 오늘의 대여 현황 제목과 기준일
  · 오른쪽 3열/6열 압축형 현황 셀
  · 기존 높이, 간격, 배경색과 반응형 구조
- 레이아웃과 라벨은 이전 사용자 홈 형태를 유지하고 아이콘만 아래 기준으로 교체했습니다.
  · 보유 자산: Boxes
  · 대여 가능: PackageCheck
  · 승인 대기: ClipboardCheck
  · 예약중: CalendarClock
  · 대여중: PackageOpen
  · 반납 지연: XCircle
- 관리자 전체 메뉴 상단과 사용자 대여 신청 상단은 RentalStatusBoard 공통 컴포넌트를 계속 사용합니다.
- 대여기간·연장기간 계산과 Firestore Rules는 변경하지 않았습니다.

배포 확인
1. 사용자 홈의 오늘의 대여 현황이 기존 가로형 전용 레이아웃으로 표시되는지 확인합니다.
2. 화면 폭이 좁을 때 현황 셀이 3열, 넓을 때 6열로 표시되는지 확인합니다.
3. 승인 대기 아이콘이 ClipboardCheck로 표시되는지 확인합니다.
4. 관리자 상단과 사용자 대여 신청 상단의 공통 카드 UI가 그대로 유지되는지 확인합니다.

--- 이전 배포 안내 ---

대여 당일 제외·연장 달력일 전환·대여 현황 공통화 배포 안내

기준 패키지
- holiday_content_top_alignment_fix_deployment_package.zip

이번 변경 파일
- src/App.jsx
- src/admin/AdminDashboardPanel.jsx
- src/admin/AdminExtensionSettingsPanel.jsx
- src/admin/AdminHolidayManagementPanel.jsx
- src/admin/AdminRequestsPanel.jsx
- src/components/RentalStatusBoard.jsx (신규)
- src/dialogs/AppDialogs.jsx
- src/user/UserHomePanel.jsx
- src/user/UserRentalPanel.jsx
- src/user/UserRequestHistoryPanel.jsx

수정 내용
1. 최초 대여 기간 계산
- 대여 시작일 당일은 기간 계산에서 제외합니다.
- 최대 대여 기간이 14일이면 대여 시작일에 14일을 더한 날짜를 기본 반납 예정일로 계산합니다.
- 계산된 마지막 날이 설정된 휴무 요일 또는 등록 휴일이면 다음 영업일로 자동 조정합니다.
- 사용자 신규 신청, 시작일 변경, 반납일 최대값, 신청 저장 검증, 사용자 신청정보 수정, 관리자 승인 처리와 관리자 대시보드 기본 일정이 같은 공통 계산 함수를 사용합니다.
- 당일 대여·당일 반납은 계속 허용합니다.

2. 대여 연장 계산
- 기존 영업일 누적 계산을 제거했습니다.
- 직전 반납 예정일 다음 날을 연장 1일 차로 하여 달력일 기준으로 계산합니다.
- 연장 기간 중 토요일·일요일·등록 휴일은 일수에 포함합니다.
- 계산된 연장 마지막 날이 휴무일인 경우에만 다음 영업일로 조정합니다.
- 휴일 조정이 완료된 최종 반납 예정일까지 예약 충돌을 검사합니다.
- 사용자 신청, 자동 승인, 관리자 승인과 승인 대기 연장 표시가 같은 공통 계산 함수를 사용합니다.
- 배포 전에 접수된 승인 대기 연장은 기존 extensionBusinessDays 숫자를 달력일 수로 해석하여 다시 계산합니다.
- 이미 승인된 기존 대여와 연장 이력의 확정 반납일은 변경하지 않습니다.

3. 연장 설정 필드 호환
- 신규 표준 설정 필드: rentalExtensionDays
- 신규 연장 신청·이력 필드: extensionDays
- 기존 rentalExtensionBusinessDays와 extensionBusinessDays는 읽기 호환용 fallback으로 유지합니다.
- 설정 화면의 회당 연장 기간 단위를 영업일에서 달력 기준 일수로 변경했습니다.

4. 대여 현황 UI 공통화
- 신규 공통 컴포넌트 RentalStatusBoard.jsx를 추가했습니다.
- 관리자 전체 메뉴 상단, 사용자 대여 신청 상단, 사용자 홈의 오늘의 대여 현황이 동일한 카드·아이콘·라벨·순서를 사용합니다.
- 승인 대기중 아이콘을 ClipboardCheck로 통일했습니다.
- 카드 전체 배경은 모두 흰색이며 상태별 색상은 아이콘 영역에만 적용됩니다.
- 사용자 홈의 기존 전용 현황 셀 구조는 제거하고 관리자 상단과 같은 6개 카드 구조로 변경했습니다.

Firestore Rules
- rules/firestore.rules는 기준본과 바이트 단위로 동일합니다.
- 이번 변경만을 위해 Rules를 다시 게시할 필요는 없습니다.

배포 순서
1. 배포 패키지의 src 폴더를 프로젝트 src 폴더에 반영합니다.
2. 프로젝트를 빌드하고 배포합니다.
3. 최대 대여 기간 14일에서 시작일 2026-07-28의 최장 반납 예정일이 2026-08-11로 계산되는지 확인합니다.
4. 계산된 마지막 날이 휴무일이면 다음 영업일로 이동하는지 확인합니다.
5. 연장 기간 5일에서 직전 반납일 다음 날부터 달력일 5일을 계산하고 최종일만 영업일 조정하는지 확인합니다.
6. 관리자 승인 대기 연장과 사용자 신청내역에 재계산된 기간이 동일하게 표시되는지 확인합니다.
7. 관리자 전체 메뉴, 사용자 대여 신청, 사용자 홈의 현황 카드가 동일한 아이콘과 흰색 카드 스타일로 표시되는지 확인합니다.

--- 이전 배포 안내 ---

관리자 로그인 대시보드 고정·휴일 달력 기본/표시 개선 배포 안내

기준 패키지
- member_policy_toggle_restore_fix_deployment_package.zip

이번 변경 파일
- src/App.jsx
- src/admin/AdminHolidayManagementPanel.jsx

수정 내용
1. 관리자 로그인 후 실시간 대시보드 고정
- 관리자 로그인 성공 시 기존 setAdminTab('dashboard') 처리를 유지합니다.
- 관리자 계정 목록 구독이 로그인 직후 일시적으로 빈 배열을 반환할 때 관리자 ID 관리 탭으로 강제 이동시키던 useEffect를 제거했습니다.
- 관리자 로그인 자체가 adminAccounts/{uid} 문서 존재와 UID 일치를 먼저 검증하므로, 해당 강제 이동은 초기 관리자 등록 보호에 필요하지 않고 정상 로그인 경로와 충돌했습니다.
- 이제 로그인 직후와 기존 관리자 세션 복원 시 기본 관리자 탭은 실시간 대시보드로 유지됩니다.
- 관리자 ID 관리는 사이드바에서 사용자가 직접 선택한 경우에만 열립니다.

2. 등록 휴일 조회 기본 화면
- holidayManagementView 초기값을 calendar로 변경했습니다.
- 이전 브라우저 localStorage에 list가 저장되어 있어도 목록 화면으로 시작하지 않도록 휴일 조회 모드 저장 처리를 제거했습니다.
- 목록/달력 전환 버튼은 그대로 유지되며 현재 휴일 관리 화면에서는 사용자가 선택한 화면을 유지합니다. 다른 관리자 메뉴로 이동했다가 휴일 관리에 다시 들어오면 달력 모드로 시작합니다.

3. 달력 셀의 휴일 표시 위치
- 날짜를 absolute 위치에서 일반 상단 행으로 변경했습니다.
- 휴일 사유 영역은 날짜 행 바로 아래에 mt-2 간격으로 배치했습니다.
- 같은 날짜에 휴일 사유가 2개이면 기존 순서대로 위에서 아래로 연속 표시됩니다.
- 휴일 유형 배지, 명칭, 색상과 상세 수정 패널 동작은 변경하지 않았습니다.

4. 오늘 날짜 강조 방식
- 검은색 ring 테두리를 제거했습니다.
- 오늘 날짜 숫자 영역에 옅은 노란색 배경(bg-yellow-100), 주황색 실선(border-orange-400), 약한 그림자를 적용했습니다.
- 평일은 기존 검정 계열, 토요일은 파랑, 일요일과 등록 휴일은 빨강 계열 날짜 색상을 그대로 유지합니다.

변경하지 않은 범위
- 관리자 Firebase Auth 로그인 검증과 UID 일치 검사
- 관리자 계정 잠금·세션 만료·로그아웃 처리
- 휴일 등록·수정·삭제·저장 로직
- 공휴일 JSON 불러오기와 휴일 병합 처리
- 대여 시작일·반납일의 휴일 계산 로직
- Firestore 문서 구조와 Firestore Rules

배포 순서
1. 배포 패키지의 src 폴더를 프로젝트 src 폴더에 반영합니다.
2. Firestore Rules는 기준본과 동일하므로 이번 수정만을 위해 재게시할 필요가 없습니다.
3. 프로젝트를 빌드하고 배포합니다.
4. 관리자 모드에서 로그아웃한 뒤 다시 로그인하여 실시간 대시보드가 첫 화면으로 표시되는지 확인합니다.
5. 대여 운영 > 휴일 관리 > 등록 휴일 조회가 달력 화면으로 시작하는지 확인합니다.
6. 휴일 사유가 날짜 바로 아래에 표시되고 같은 날짜의 사유 2개가 세로로 연속 표시되는지 확인합니다.
7. 오늘 날짜가 옅은 노란색 바탕과 주황색 실선으로 표시되고 요일별 날짜 색상이 유지되는지 확인합니다.

--- 이전 배포 안내 ---

회원가입 정책 토글 표시·명부 불일치 계정 자동 복원 수정 배포 안내

기준 패키지
- home_popup_request_layout_fix_deployment_package(1).zip

이번 변경 파일
- src/admin/AdminSignupPolicyPanel.jsx
- src/App.jsx
- rules/firestore.rules

수정 내용
1. 회원가입 정책 토글 표시
- 토글 손잡이의 절대 위치 기준에 left-0을 추가했습니다.
- 꺼짐 상태는 왼쪽, 켜짐 상태는 오른쪽에 정확히 표시됩니다.
- 기존 색상, 크기, 비활성화 처리, 클릭 동작은 변경하지 않았습니다.

2. 가입 제한 정책 해제 시 기존 회원 복원
- 등록 명부 불일치로 profileRequired 상태가 된 회원만 복원 대상으로 제한합니다.
- 가입 제한 정책이 꺼진 상태에서 관리자 회원 목록이 로드되면 남아 있는 명부 불일치 회원을 자동 복원합니다.
- 정책 저장 시에도 정책이 꺼진 상태라면 명부 불일치 회원 복원을 다시 실행합니다.
- statusBeforeProfileRequired가 active 또는 pending이면 원래 상태를 그대로 복원합니다.
- 구버전 데이터처럼 이전 상태가 비어 있거나 잘못된 경우에는 active로 복원합니다.
- 부서·성명 중복, 관리자 수동 이용 차단, 이용 종료 상태는 자동 복원하지 않습니다.

3. 로그인·기존 세션 복원
- profileRequiredReason이 directoryMismatch인 계정은 로컬 정책 버전 값과 관계없이 서버의 최신 정책을 다시 확인합니다.
- 정책이 꺼져 있으면 로그인 과정에서 계정 상태와 이메일 찾기용 복구키 상태를 함께 복원합니다.
- 명부 불일치 때문에 마이페이지로 강제 이동됐던 사용자가 active로 복원되면 기기 대여 화면으로 이동합니다.

4. Firestore Rules
- statusBeforeProfileRequired가 active 또는 pending이면 해당 값을 사용합니다.
- 값이 없거나 유효하지 않으면 active를 복원 상태로 인정합니다.
- 앱의 복원 상태 계산과 Rules의 허용 상태 계산을 동일하게 맞췄습니다.

배포 순서
1. 배포 패키지의 src 폴더를 프로젝트 src 폴더에 반영합니다.
2. rules/firestore.rules를 프로젝트 Firestore Rules 전체 교체본으로 반영합니다.
3. Firebase Console 또는 Firebase CLI에서 Firestore Rules를 반드시 게시합니다.
4. 프로젝트를 빌드하고 배포합니다.
5. 관리자 로그인 후 회원가입 정책 화면에서 토글 꺼짐 위치를 확인합니다.
6. 등록된 부서·사용자만 가입 허용을 끈 상태에서 명부 불일치 회원이 active 또는 기존 pending 상태로 복원되는지 확인합니다.
7. 해당 사용자가 다시 로그인했을 때 불필요한 마이페이지 강제 이동 없이 정상 이용 화면으로 진입하는지 확인합니다.

주의
- 이번 수정은 Firestore Rules 변경을 포함하므로 src만 교체하면 사용자 본인 복원이 permission-denied로 실패할 수 있습니다.
- 정책 해제 자동 복원 대상은 profileRequiredReason == directoryMismatch인 계정뿐입니다.

--- 이전 배포 안내 ---

홈 화면 관리 제목 계층·팝업/신청 목록 너비 보정 추가 배포 안내

기준 패키지
- home_footer_popup_management_deployment_package.zip

이번 추가 변경
1. 팝업 관리 목록
- PC 화면에서 가로 스크롤을 만들던 900px 최소 너비를 제거했습니다.
- 번호·순서·사용·제목·노출 페이지·노출 기간·상태·등록일·관리 열을 비율형 colgroup으로 재배분했습니다.
- 제목·부제목 열은 기존보다 조금 줄이고 순서·관리 버튼과 상태 배지가 잘리지 않는 폭을 확보했습니다.
- lg 이상 화면에서는 표를 현재 콘텐츠 폭에 맞추고, 작은 화면에서만 최소 너비와 가로 스크롤을 유지합니다.

2. 홈 화면 관리 제목 구조
- 검은 그라데이션 대제목은 내부 탭마다 반복하지 않고 '홈 화면 관리' 하나로 통일했습니다.
- 세부 탭 내비게이션을 홈 화면 관리 대제목 바로 아래로 이동했습니다.
- 홈 화면 기본 설정, 메인 비주얼 관리, 프로모션 배너 관리, 바로가기 배너 관리 제목은 탭 아래에서 대여 정책 관리의 섹션 제목과 같은 스타일로 표시합니다.
  · text-base
  · font-bold
  · text-slate-900
  · 설명 text-xs / text-slate-500

3. 기기 대여 신청 관리 목록
- 기기명 17%→15%, 신청자 11%→10%, 부서명 14%→12%로 축소했습니다.
- 상태 열 9%→12%로 확대하고 접수·처리일과 비고 열도 재배분했습니다.
- 상태 배지에 whitespace-nowrap을 적용하여 '반납완료', '연체반납', '사용자 취소' 같은 상태가 한 줄로 표시됩니다.

변경하지 않은 범위
- 팝업 순서·필터·페이지당 보기 기능
- 홈 화면 배너 저장·정렬·모달·링크 기능
- 대여 신청 상태 계산과 데이터 처리 로직
- Firestore 문서 구조와 Rules 권한

배포 순서
1. 배포 패키지의 src 폴더를 프로젝트 src 폴더에 반영합니다.
2. rules/firestore.rules는 기준본과 동일하므로 기존 최신 Rules가 게시되어 있다면 재게시할 필요가 없습니다.
3. 로컬 프로젝트에서 빌드합니다.
4. PC 화면에서 팝업 관리 표의 가로 스크롤이 사라졌는지 확인합니다.
5. 홈 화면 관리의 제목 → 탭 → 세부 제목 순서를 확인합니다.
6. 기기 대여 신청 관리의 모든 상태 배지가 한 줄로 표시되는지 확인합니다.

--- 이전 배포 안내 ---

사이트 관리 홈 화면 통합·배너/푸터 모달·팝업 순서 관리 배포 안내

기준 패키지
- banner_footer_carousel_ux_fix_deployment_package.zip

배포 순서
1. 배포 패키지의 src 폴더를 프로젝트 src 폴더에 반영합니다.
2. 배포 패키지의 rules/firestore.rules를 로컬·원격 프로젝트의 rules/firestore.rules에 반영합니다.
3. 이번 변경에서 Firestore Rules 내용은 기준본과 동일하므로 기존 최신 Rules가 게시되어 있다면 재게시가 필수는 아닙니다.
4. 프로젝트를 빌드하고 배포합니다.
5. 관리자 모드 > 사이트 관리에서 아래 기능을 확인합니다.

홈 화면 관리 통합
- 기존 홈 화면 기본 설정, 메인 비주얼 관리, 프로모션 배너 관리, 바로가기 배너 관리를 '홈 화면 관리' 하나로 통합했습니다.
- 홈 화면 관리 내부 탭:
  · 홈 화면 기본 설정
  · 메인 비주얼
  · 프로모션 배너
  · 바로가기 배너
- 기존 메뉴 키로 진입하면 대응하는 내부 탭으로 자동 이동합니다.

배너 편집 모달
- 메인 비주얼, 프로모션 배너, 바로가기 배너 등록·수정을 화면 중앙 모달로 표시합니다.
- 기존 이미지 표시 방식, 초점 위치, 링크 방식, 현재 탭/새 탭, 노출 기간 기능은 유지합니다.

푸터 편집 UX
- 푸터 메뉴 페이지 등록·수정을 화면 중앙 모달로 표시합니다.
- 푸터 전용 제목 표시 방식은 유지합니다.
  · 텍스트 제목
  · 이미지 제목
- 연결 입력 영역은 배너 관리와 같은 20% / 20% / 60% 구조입니다.
  · 연결 방식: 본문 직접 입력 / 외부 링크 / 링크 없음
  · 탭 열기: 현재 탭 / 새 탭
  · 연결 대상: 외부 링크 주소 또는 상태 안내
- 더 이상 링크 없음 처리를 위해 #을 입력할 필요가 없습니다.
- 기존 linkUrl 값이 #인 문서는 수정 화면에서 자동으로 '링크 없음'으로 해석합니다.

팝업 순서와 목록 개수
- 팝업 목록에 위/아래 순서 이동 버튼을 추가했습니다.
- 팝업 sortOrder는 관리자 목록과 사용자 노출 순서에 함께 적용됩니다.
- 기존 sortOrder가 없는 팝업은 생성일 기준으로 표시되며, 저장·이동·삭제 시 연속 순번으로 정규화됩니다.
- 한 번에 보기: 10개 / 30개 / 50개 / 100개
- 노출 페이지, 노출 기간, 현재 상태, 등록일 열 너비를 줄여 순서 열을 추가했습니다.

운영 확인 시나리오
1. 홈 화면 관리의 네 내부 탭을 전환합니다.
2. 각 배너 종류에서 등록·수정 모달이 화면 중앙에 표시되는지 확인합니다.
3. 푸터에서 본문 직접 입력, 외부 링크, 링크 없음 세 방식을 각각 저장합니다.
4. 외부 링크를 현재 탭과 새 탭으로 각각 열어 봅니다.
5. 기존 # 방식 푸터 문서를 수정한 뒤 링크 없음으로 저장되는지 확인합니다.
6. 팝업 12개 이상으로 위/아래 이동과 10·30·50·100개 보기를 확인합니다.
7. 사용자 화면에서 팝업이 관리자 순서대로 표시되는지 확인합니다.

변경하지 않은 범위
- 대여 신청·승인·반납 로직
- 회원가입·회원 계정·관리자 인증 로직
- Firestore 컬렉션 경로와 Rules 권한
- 팝업 등록 모달의 본문·노출 페이지·노출 기간 입력 기능
- 푸터 공통 정보 저장 기능
